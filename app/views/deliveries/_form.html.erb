<%= form_for(delivery) do |f| %>
  <% if delivery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery.errors.count, "error") %> prohibited this delivery method from being saved:</h2>

      <ul>
      <% delivery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset>
    <legend>Wpisz dane dostawy</legend>
    
    <div class="field form-group">
      <%= f.label :name, 'Nazwa'%>
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  
    <div class="field form-group">
      <%= f.label :price, 'Cena'%>
      <%= f.text_field :price, class: 'form-control' %>
    </div>
  
    <div class="actions">
      <%= f.submit 'Wyślij', class: 'btn btn-primary form-control' %>
    </div>
  </fieldset>
<% end %>

<%= link_to 'Cofnij do listy dostaw', deliveries_path, class: 'btn btn-default' %>
